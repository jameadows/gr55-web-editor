/**
 * Core Services Public API
 * 
 * Barrel export for clean imports in components.
 * 
 * Usage:
 * import { KeyboardShortcutService, ConfirmationDialogService } from '@app/core/services';
 */

export * from './keyboard-shortcut.service';
export * from './confirmation-dialog.service';
